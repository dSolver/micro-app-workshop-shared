// This is the entry point for the application
// We use dynamic imports to load the bootstrap file
// This way the application is split into chunks

// As an MFE, this file is not used, but if this was a standalone application, this would be the entry point
import('./bootstrap');